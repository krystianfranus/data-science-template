defaults:
  - _self_
  - model: default.yaml
  - override hydra/hydra_logging: none
  - override hydra/job_logging: stdout

hydra:
  output_subdir: null
  run:
    dir: .

prev_task_id: ???
num_workers: 0
pin_memory: false
