defaults:
  - _self_
  - model: default.yaml
  - override hydra/hydra_logging: none
  - override hydra/job_logging: stdout

hydra:
  output_subdir: null
  run:
    dir: .

num_workers: 0
pin_memory: false