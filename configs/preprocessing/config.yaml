defaults:
  - _self_
  - override hydra/hydra_logging: none
  - override hydra/job_logging: stdout

hydra:
  output_subdir: null
  run:
    dir: .

cwd: ${hydra:runtime.cwd}
raw_data_path: ${cwd}/data/movielens/ratings.dat
data_type: "implicit"
execute_remotely: true
