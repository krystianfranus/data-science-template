# @package _global_

defaults:
  - override /datamodule: simple
  - override /model: simple_mf

datamodule:
  num_workers: 8
  pin_memory: true

model:
  embed_size: 24
  lr: 1e-3

trainer:
  accelerator: "gpu"
  max_epochs: 3
