# @package _global_

defaults:
  - override /datamodule: simple
  - override /net: mlp  # mf
  - override /optimizer: adam
  - override /model: reg

data_type: explicit

datamodule:
  num_workers: 6
  pin_memory: true

trainer:
  accelerator: gpu
