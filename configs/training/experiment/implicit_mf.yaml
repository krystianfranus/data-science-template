# @package _global_

defaults:
  - override /datamodule: simple
  - override /model: cla_mf
  - override /net: mf

datamodule:
  num_workers: 6
  pin_memory: true

model:
  lr: 1e-3

net:
  embed_size: 24

trainer:
  accelerator: gpu
