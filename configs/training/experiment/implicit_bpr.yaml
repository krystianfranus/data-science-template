# @package _global_

defaults:
  - override /datamodule: bpr
  - override /net: mlp  # mf
  - override /optimizer: adam
  - override /model: bpr

data_type: implicit_bpr

datamodule:
  num_workers: 6
  pin_memory: true

trainer:
  accelerator: gpu
