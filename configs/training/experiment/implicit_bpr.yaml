# @package _global_

defaults:
  - override /datamodule: bpr
  - override /model: bpr
  - override /net: mlp  # mf

data_type: "implicit_bpr"
clearml: true

datamodule:
  num_workers: 6
  pin_memory: true

trainer:
  accelerator: gpu
