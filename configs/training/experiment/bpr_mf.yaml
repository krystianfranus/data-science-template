# @package _global_

defaults:
  - override /datamodule: bpr
  - override /model: bpr_mf
  - override /net: mf

datamodule:
  num_workers: 8
  pin_memory: true

model:
  lr: 1e-3

net:
  embed_size: 24

trainer:
  accelerator: gpu
  max_epochs: 3
