# @package _global_

defaults:
  - override /datamodule: simple
  - override /model: simple_mlp

datamodule:
  num_workers: 8
  pin_memory: true

model:
  n_factors: 8
  n_layers: 3
  dropout: 0.0
  lr1: 0.00001
  lr2: 0.00005
  weight_decay: 0.01

trainer:
  accelerator: "gpu"
  max_epochs: 3
