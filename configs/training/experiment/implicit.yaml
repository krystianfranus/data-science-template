# @package _global_

defaults:
  - override /datamodule: simple
  - override /net: mlp  # mf
  - override /optimizer: adam
  - override /model: cla

data_type: implicit

datamodule:
  num_workers: 6
  pin_memory: true

trainer:
  accelerator: gpu
