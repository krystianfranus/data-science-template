# @package _global_

defaults:
  - override /datamodule: simple
  - override /net: mlp  # mf
  - override /model: cla_mlp  # cla_mf

data_type: "implicit"

datamodule:
  num_workers: 6
  pin_memory: true

trainer:
  accelerator: gpu
