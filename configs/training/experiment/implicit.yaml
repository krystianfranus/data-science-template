# @package _global_

defaults:
  - override /datamodule: simple
  - override /model: cla_mlp  # cla_mf
  - override /net: mlp  # mf

data_type: "implicit"
clearml: true

datamodule:
  num_workers: 6
  pin_memory: true

trainer:
  accelerator: gpu
