defaults:
  - _self_
  - datamodule: simple
  - model: mf_sim_reg
  - callbacks: default
  - trainer: default

  - experiment: null

  - override hydra/hydra_logging: none
  - override hydra/job_logging: stdout

hydra:
  output_subdir: null
  run:
    dir: .

data_type: "explicit"
execute_locally: false
draft_mode: false
prev_task_id: null
