defaults:
  - _self_
  - datamodule: simple
  - model: mf_sim_reg
  - callbacks: default
  - trainer: default

  - experiment: reg

  - override hydra/hydra_logging: none
  - override hydra/job_logging: stdout

hydra:
  output_subdir: null
  run:
    dir: .

data_type: "explicit"
execute_locally: true
draft_mode: false
prev_task_id: null
